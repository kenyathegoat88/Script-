local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()

local screenGui = Instance.new("ScreenGui", LocalPlayer:WaitForChild("PlayerGui"))
screenGui.Name = "TridenteKT7_GUI"
screenGui.ResetOnSpawn = false

local openBtn = Instance.new("TextButton", screenGui)
openBtn.Size = UDim2.new(0, 180, 0, 40)
openBtn.Position = UDim2.new(0, 10, 0, 10)
openBtn.Text = "👹 Abrir Tridente KT7"
openBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
openBtn.TextColor3 = Color3.new(1, 1, 1)
openBtn.Font = Enum.Font.GothamBold
openBtn.TextSize = 16
openBtn.Visible = false

local mainFrame = Instance.new("Frame", screenGui)
mainFrame.Size = UDim2.new(0, 330, 0, 400)
mainFrame.Position = UDim2.new(0.5, -165, 0.5, -200)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mainFrame.BorderSizePixel = 0
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)

-- Draggable para móvil y PC
local UIS = game:GetService("UserInputService")
local dragging, dragInput, dragStart, startPos
local function update(input)
	local delta = input.Position - dragStart
	mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X,
		startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end
mainFrame.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragStart = input.Position
		startPos = mainFrame.Position
		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then dragging = false end
		end)
	end
end)
mainFrame.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.Touch or input.UserInputType == Enum.UserInputType.MouseMovement then
		dragInput = input
	end
end)
UIS.InputChanged:Connect(function(input)
	if input == dragInput and dragging then update(input) end
end)

-- Título
local title = Instance.new("TextLabel", mainFrame)
title.Size = UDim2.new(1, 0, 0, 40)
title.Position = UDim2.new(0, 0, 0, 5)
title.BackgroundTransparency = 1
title.Text = "Tridente KT7"
title.Font = Enum.Font.GothamBold
title.TextSize = 26
title.TextColor3 = Color3.new(1, 1, 1)

-- Subtítulo
local sub = Instance.new("TextLabel", mainFrame)
sub.Size = UDim2.new(1, 0, 0, 25)
sub.Position = UDim2.new(0, 0, 0, 35)
sub.BackgroundTransparency = 1
sub.Text = "by kenya"
sub.Font = Enum.Font.Gotham
sub.TextSize = 18
sub.TextColor3 = Color3.fromRGB(150, 150, 150)

-- Cuerpo en scroll para evitar que se corte
local scroll = Instance.new("ScrollingFrame", mainFrame)
scroll.Size = UDim2.new(1, 0, 1, -65)
scroll.Position = UDim2.new(0, 0, 0, 65)
scroll.CanvasSize = UDim2.new(0, 0, 0, 600)
scroll.ScrollBarThickness = 6
scroll.BackgroundTransparency = 1

-- FUNC: crear casilla con label
local function crearCasilla(nombre, posicionY)
	local label = Instance.new("TextLabel", scroll)
	label.Size = UDim2.new(0.6, -10, 0, 30)
	label.Position = UDim2.new(0, 10, 0, posicionY)
	label.BackgroundTransparency = 1
	label.Text = nombre
	label.Font = Enum.Font.GothamBold
	label.TextSize = 16
	label.TextColor3 = Color3.fromRGB(255,255,255)
	label.TextXAlignment = Enum.TextXAlignment.Left

	local input = Instance.new("TextBox", scroll)
	input.Size = UDim2.new(0.35, 0, 0, 30)
	input.Position = UDim2.new(0.6, 0, 0, posicionY)
	input.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
	input.TextColor3 = Color3.new(1,1,1)
	input.Font = Enum.Font.Gotham
	input.TextSize = 16
	input.Text = "6"
	input.TextXAlignment = Enum.TextXAlignment.Center
	return input
end

-- CASILLAS
local alcance1 = crearCasilla("Alcance Reach:", 10)
local alcance2 = crearCasilla("Alcance Servicio 2:", 60)

-- BOTONES
local btn1 = Instance.new("TextButton", scroll)
btn1.Size = UDim2.new(1, -20, 0, 40)
btn1.Position = UDim2.new(0, 10, 0, 110)
btn1.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
btn1.TextColor3 = Color3.new(1, 1, 1)
btn1.Font = Enum.Font.GothamBold
btn1.TextSize = 18
btn1.Text = "✅ Activar Reach"

local btn2 = Instance.new("TextButton", scroll)
btn2.Size = UDim2.new(1, -20, 0, 40)
btn2.Position = UDim2.new(0, 10, 0, 160)
btn2.BackgroundColor3 = Color3.fromRGB(255, 100, 0)
btn2.TextColor3 = Color3.new(1, 1, 1)
btn2.Font = Enum.Font.GothamBold
btn2.TextSize = 18
btn2.Text = "✅ Activar Servicio 2"

-- BOTÓN CERRAR Y MINIMIZAR
local cerrarBtn = Instance.new("TextButton", mainFrame)
cerrarBtn.Size = UDim2.new(0, 80, 0, 25)
cerrarBtn.Position = UDim2.new(1, -90, 0, 10)
cerrarBtn.BackgroundColor3 = Color3.fromRGB(150, 40, 40)
cerrarBtn.Text = "Cerrar"
cerrarBtn.Font = Enum.Font.GothamBold
cerrarBtn.TextSize = 14
cerrarBtn.TextColor3 = Color3.new(1, 1, 1)

local minimizarBtn = Instance.new("TextButton", mainFrame)
minimizarBtn.Size = UDim2.new(0, 80, 0, 25)
minimizarBtn.Position = UDim2.new(1, -180, 0, 10)
minimizarBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
minimizarBtn.Text = "Minimizar"
minimizarBtn.Font = Enum.Font.GothamBold
minimizarBtn.TextSize = 14
minimizarBtn.TextColor3 = Color3.new(1, 1, 1)

cerrarBtn.MouseButton1Click:Connect(function()
	mainFrame.Visible = false
	openBtn.Visible = false
end)

minimizarBtn.MouseButton1Click:Connect(function()
	mainFrame.Visible = false
	openBtn.Visible = true
end)

openBtn.MouseButton1Click:Connect(function()
	mainFrame.Visible = true
	openBtn.Visible = false
end)

-- LOGICA REACH (sin colisión, invisible)
local function activarHitbox(tamaño, color)
	local leg = Character:FindFirstChild("RightFoot") or Character:FindFirstChild("Right Leg")
	if not leg then return end
	local box = Instance.new("Part", Character)
	box.Size = Vector3.new(4, 4, tamaño)
	box.Transparency = 1
	box.CanCollide = false
	box.Anchored = false
	box.Massless = true
	box.Name = "Hitbox_" .. math.random(1, 1000)
	box.Color = color or Color3.fromRGB(255, 0, 0)

	local weld = Instance.new("WeldConstraint", box)
	weld.Part0 = box
	weld.Part1 = leg
	box.CFrame = leg.CFrame * CFrame.new(0, 0, -tamaño / 2)
end

btn1.MouseButton1Click:Connect(function()
	local valor = tonumber(alcance1.Text) or 6
	activarHitbox(valor, Color3.fromRGB(255, 0, 0))
end)

btn2.MouseButton1Click:Connect(function()
	local valor = tonumber(alcance2.Text) or 6
	activarHitbox(valor, Color3.fromRGB(0, 255, 0))
end)
